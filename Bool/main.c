#include <stdio.h>
#include <stdbool.h> // Build-in Boolean introduced in C99

int main(void)
{
    bool isClever = true; // <-- Use this library
    bool isBad = false;
    printf("IsClever: %d\n", isClever);
    printf("IsBad: %d\n", isBad);

    return 0;
}
